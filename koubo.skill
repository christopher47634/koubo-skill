# 口播视频一键生成 Skill (koubo)

> 把你的口播视频 + 稿子丢给 AI，自动产出带字幕、配图、BGM、赛博朋克 HUD 的成品视频。
> 你只需要提供两样东西：**稿子** 和 **口播视频**。其他一切由 AI 自动完成。

---

## 这个 Skill 能干什么

```
你的输入                          AI 自动完成                          成品输出
┌─────────────┐    ┌──────────────────────────────────┐    ┌─────────────────┐
│  口播视频    │    │  1. 视频转码 (H.265→H.264)       │    │  1920x1080 MP4  │
│  稿子文字    │ →  │  2. Whisper 提取时间戳            │ →  │  赛博朋克字幕    │
│             │    │  3. 字幕对齐 (锚点+插值)           │    │  关键句弹图      │
│             │    │  4. AI 分析插入点 + 搜图           │    │  BGM 循环铺底    │
│             │    │  5. 关键词高亮分类                 │    │  HUD 四角框     │
│             │    │  6. Remotion 渲染                 │    │  REC 指示灯     │
└─────────────┘    └──────────────────────────────────┘    └─────────────────┘
```

---

## 适用人群

- 有口播视频和稿子，想快速生成带字幕配图的成品视频
- 不想手动对齐字幕、找图、剪辑
- 喜欢赛博朋克 HUD 风格
- 使用 **Hermes Agent** 或 **Claude Code** 作为 AI 助手

---

## 前置条件

### 必装软件

| 软件 | 用途 | 安装方式 |
|------|------|----------|
| **Node.js** (v18+) | 运行 Remotion | https://nodejs.org |
| **Python** (3.10+) | Whisper 语音识别 | https://python.org |
| **ffmpeg** | 视频转码 | https://ffmpeg.org (或 `brew install ffmpeg` / `apt install ffmpeg`) |
| **npm** | 安装依赖 | 随 Node.js 自带 |

### 可选（强烈推荐）

| 软件 | 用途 | 说明 |
|------|------|------|
| **NVIDIA GPU + CUDA** | Whisper 加速 | 有 GPU 跑 base 模型只要几十秒，CPU 要几分钟 |
| **Hermes Agent** | AI 自动化 | https://hermes-agent.nousresearch.com |
| **Claude Code** | AI 编码助手 | `npm install -g @anthropic-ai/claude-code` |

### 字体文件（必需）

本模板使用两个字体，需要自行下载放入项目：

1. **LXGW WenKai**（中文字体）：https://github.com/lxgw/LxgwWenKai/releases
   - 下载 `LXGWWenKai-Regular.ttf`
2. **Chakra Petch**（英文字体）：https://fonts.google.com/specimen/Chakra+Petch
   - 下载 SemiBold 字重的 `.ttf` 文件

下载后放入项目的 `public/fonts/` 目录。

---

## 快速开始（3 种方式）

### 方式一：Hermes Agent（推荐，全自动）

如果你已经安装了 Hermes Agent，直接把 `koubo.skill` 放到你的 skills 目录：

```bash
# 1. 下载 skill
git clone https://github.com/christopher47634/koubo-skill.git
cp koubo-skill/koubo.skill ~/.hermes/skills/

# 2. 重启 Hermes
hermes restart

# 3. 对话中使用
# 对 Hermes 说："帮我生成一个口播视频，稿子是 [你的稿子]，视频在 [路径]"
```

Hermes 会自动执行全部流程：转码、whisper、对齐、搜图、渲染。

### 方式二：Claude Code（半自动）

```bash
# 1. 下载 skill
git clone https://github.com/christopher47634/koubo-skill.git

# 2. 在项目目录启动 Claude Code
cd koubo-skill
claude

# 3. 对 Claude 说：
# "按照 koubo.skill 的流程，帮我生成口播视频。稿子是 [你的稿子]，视频在 [路径]"
```

Claude Code 会按照 skill 中的步骤逐步执行。

### 方式三：手动执行（了解原理）

如果你想了解每一步在做什么，可以手动执行。见下方"详细工作流"。

---

## 详细工作流（7 步）

### Step 1: 初始化项目

```bash
# 创建项目目录
mkdir -p my-video/{input,public/{fonts,images,videos,music,sfx},src}
cd my-video

# 初始化 Node.js 项目
npm init -y
npm install react react-dom remotion @remotion/cli @remotion/bundler @remotion/renderer @remotion/media-utils typescript @types/react --registry=https://registry.npmmirror.com

# 创建 tsconfig.json
cat > tsconfig.json << 'EOF'
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "jsx": "react-jsx",
    "strict": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
EOF
```

### Step 2: 放入素材

```
my-video/
├── input/
│   └── your-video.mp4          ← 把你的口播视频放这里
├── public/
│   ├── fonts/
│   │   ├── LXGWWenKai-Regular.ttf    ← 中文字体
│   │   └── ChakraPetch-SemiBold.ttf  ← 英文字体
│   ├── music/
│   │   └── bgm.mp3             ← BGM 文件（可选，有默认）
│   ├── images/                 ← AI 会自动搜图放这里
│   └── videos/                 ← AI 会自动搜视频放这里
└── src/
    ├── index.ts
    ├── Root.tsx
    ├── Composition.tsx
    ├── subtitles.ts
    └── subtitles.json
```

### Step 3: 视频转码

如果视频是 H.265 编码（大部分手机录的都是），需要转成 H.264：

```bash
ffmpeg -i input/your-video.mp4 -c:v libx264 -crf 18 -preset fast -c:a aac -b:a 128k public/input-video.mp4
```

检查视频时长（后面要用）：
```bash
ffprobe -v error -show_entries format=duration -of csv=p=0 public/input-video.mp4
```

### Step 4: Whisper 提取时间戳

```bash
pip install faster-whisper
```

```python
# run_whisper.py
from faster_whisper import WhisperModel
import json

# 有 GPU 用 "base"，没 GPU 也用 "base"（CPU 慢但能跑）
model = WhisperModel("base", device="cuda")  # 没GPU改成 device="cpu"
segments, info = model.transcribe("public/input-video.mp4", language="zh")

result = [{"start": s.start, "end": s.end, "text": s.text} for s in segments]
with open("whisper_output.json", "w", encoding="utf-8") as f:
    json.dump(result, f, ensure_ascii=False, indent=2)

print(f"识别完成：{len(result)} 个片段")
```

```bash
python run_whisper.py
```

### Step 5: 字幕对齐

**核心思路**：Whisper 识别对的句子当锚点，识别错的按字数在锚点间插值。

1. 对比 `whisper_output.json` 和你的稿子
2. Whisper 识别正确的句子 → 直接用它的时间戳
3. Whisper 识别错误的句子 → 按字数在两个锚点之间插值
4. 长句可以拆成两句分别跟踪
5. 每句最少 0.3 秒，不能有重叠

生成 `subtitles.json` 并复制到 `src/`：

```bash
cp subtitles.json src/subtitles.json
```

### Step 6: AI 分析 + 搜图 + 更新代码

这一步由 AI 自动完成：

1. **分析稿子**：为每句话判断是否需要配图
2. **搜图下载**：根据说话内容搜索精准对应的图片
3. **更新 SCHEDULE**：把图片/视频调度表写入 `Composition.tsx`
4. **更新 KW**：把关键词高亮分类写入 `Composition.tsx`

### Step 7: 渲染输出

```bash
# 更新 Root.tsx 中的 durationInFrames（视频秒数 × 30）
# 然后渲染
TMPDIR=/tmp npx remotion render src/index.ts main output.mp4 --codec h264
```

渲染完成后，`output.mp4` 就是你的成品视频。

---

## 成品效果

### 字幕
- 60px 大字，底部居中
- 半透明黑底 + 青色边框
- 关键词自动变色：
  - 🔵 蓝色 = 工具/产品名（ChatGPT、豆包）
  - 🟠 橙色 = 概念/理念（AI、认知、风口）
  - 🟣 紫色 = 平台/场景（小红书、直播、星巴克）
  - 🔴 红色 = 陷阱/负面（改命、被骗、离谱）
  - 🟢 绿色 = 金钱/数字（月入十万、3980）

### 配图
- 右侧弹入，540x405 大图
- 发光边框 + 角标
- spring 弹性动画入场
- 关键句才弹图，不是每句都有
- 后半段频率更高

### HUD
- 四角青色边框
- REC 红色闪烁指示灯
- 扫描线效果
- 帧数显示

### BGM
- 8% 音量循环铺底
- 保留口播原声

---

## 文件结构详解

```
koubo-skill/
├── koubo.skill                   ← 本文件（skill 主文档）
├── templates/
│   ├── Composition.tsx           ← Remotion 主组件模板
│   ├── Root.tsx                  ← Remotion Root 模板
│   └── subtitles.ts              ← 字幕类型定义
├── scripts/
│   ├── align_subtitles.py        ← 字幕对齐脚本模板
│   ├── init_project.sh           ← 项目初始化脚本
│   └── run_whisper.py            ← Whisper 运行脚本
└── references/
    ├── keyword-highlight.md      ← 关键词高亮系统说明
    ├── image-download-workflow.md ← 图片搜索策略
    └── pitfalls.md               ← 常见问题排查
```

---

## 常见问题

### Q: 没有 GPU 能用吗？
A: 能。Whisper base 模型在 CPU 上也能跑，只是慢一些（2 分钟视频大概要 3-5 分钟）。渲染不需要 GPU。

### Q: 视频是什么格式都行吗？
A: 输入任意格式，但 H.265 编码的需要先转 H.264（ffmpeg 一行命令）。H.264、VP9 等可以直接用。

### Q: 字幕时间不准怎么办？
A: Whisper base 的时间戳是粗略的，需要手动校准。AI 会自动做锚点+插值对齐，但建议人工检查关键句子的时间。

### Q: 图片是自动找的吗？
A: 是的。AI 分析你的稿子内容，自动搜索对应图片。品牌名搜品牌图，场景描述搜场景图，抽象概念搜视觉隐喻。

### Q: 能换个 BGM 吗？
A: 能。把你的 BGM 文件放到 `public/music/` 目录，然后修改 `Composition.tsx` 中的文件名。

### Q: 能改字幕样式吗？
A: 模板参数是锁死的（60px 居中、半透明背景等）。如果要改，直接编辑 `Composition.tsx` 中对应的位置。但一般不需要改。

### Q: 渲染很慢怎么办？
A: 2 分钟视频在 RTX 4070 上大概 3-5 分钟。没有 GPU 会更慢。可以降低帧率（fps）或分辨率来加速，但会损失质量。

### Q: 支持英文稿子吗？
A: 目前模板针对中文优化（LXGW WenKai 字体）。英文稿子需要换字体，并调整 whisper 的 language 参数。

### Q: 怎么批量生成多个视频？
A: 每个视频需要独立的项目目录。可以写脚本循环执行 Step 3-7。

---

## 技术栈

- **Remotion** v4：用 React 写视频
- **Whisper** (faster-whisper)：语音识别提取时间戳
- **React** 18 + TypeScript
- **ffmpeg**：视频转码
- **Node.js** + npm

---

## 许可

MIT License. 自由使用、修改、分发。

---

## 致谢

- Remotion 团队：https://www.remotion.dev
- LXGW WenKai 字体：https://github.com/lxgw/LxgwWenKai
- faster-whisper：https://github.com/SYSTRAN/faster-whisper

---

*Made with ❤️ by 百灵鸟 (Songbird)*
